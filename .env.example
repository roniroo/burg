# Supabase project: burg (ybquniffzetaylkrkadz)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. Never expose to the browser. Used by scripts/seed.ts and by the
# first-sign-in seeding path, both of which write across RLS boundaries.
SUPABASE_SERVICE_ROLE_KEY=

# Absolute origin used to build auth redirect URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
